[project]
name = "graphpaper"
version = "0.1.0"
description = "PDF utilities for graph paper, dot grids, and calendar generation"
requires-python = ">=3.14"
dependencies = [
    "tkinterdnd2",
    "reportlab",
    "PyPDF2",
    "pycryptodome",
]
